import { CalculTtcDirective } from './calcul-ttc.directive';

describe('CalculTtcDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculTtcDirective();
    expect(directive).toBeTruthy();
  });
});
